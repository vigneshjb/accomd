<%= form_for(@accomadation) do |f| %>
  <% if @accomadation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accomadation.errors.count, "error") %> prohibited this accomadation from being saved:</h2>

      <ul>
      <% @accomadation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :isBooked %><br />
    <%= f.check_box :isBooked %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
